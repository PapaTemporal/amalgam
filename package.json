{
  "name": "@papatemporal/amalgam",
  "version": "0.1.0",
  "private": true,
  "description": "Local offload stack for frontier-model coding agents: SQLite layered memory with semantic recall, a code graph served as evidence packets, work items, and optional local-model digest. Zero installs, zero admin, zero cloud.",
  "type": "module",
  "bin": {
    "amalgam": "bin/amalgam.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/papatemporal/amalgam.git"
  },
  "license": "MIT",
  "scripts": {
    "test": "node tests/recall-eval.mjs && node tests/memory-eval.mjs && node tests/graph-eval.mjs && node tests/graphdb-eval.mjs && node tests/task-eval.mjs && node tests/capture-eval.mjs && node tests/stream-eval.mjs"
  }
}
